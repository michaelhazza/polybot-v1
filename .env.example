# Server Configuration
PORT=3001
NODE_ENV=development

# Database
DATABASE_PATH=./data/polymarket.db

# External APIs
POLYMARKET_API_BASE=https://clob.polymarket.com

# Data Source Selection
# Set to 'true' to use Bitquery blockchain data, 'false' for Polymarket API
USE_BITQUERY=true

# Bitquery API Configuration
# Sign up at: https://ide.bitquery.io/
# Navigate to: Account → Profile → API Keys
# Copy the OAuth token (format: Bearer your-token-here)
BITQUERY_OAUTH_TOKEN=
BITQUERY_STREAMING_ENDPOINT=https://streaming.bitquery.io/graphql
BITQUERY_STANDARD_ENDPOINT=https://graphql.bitquery.io/

# Security - JWT Authentication
# IMPORTANT: Change JWT_SECRET in production to a strong, random secret
JWT_SECRET=your-secret-key-change-in-production
JWT_EXPIRES_IN=24h

# CORS Configuration
# Comma-separated list of allowed origins
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000
